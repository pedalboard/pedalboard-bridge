[package]
name = "opendeck-stub"
version = "0.1.0"
edition = "2021"

[dependencies]
opendeck = { path = "../../opendeck" }
midi2 = { version = "0.11", default-features = false, features = ["sysex7"] }
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = "0.24"
futures-util = "0.3"
