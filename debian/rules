#!/usr/bin/make -f
%:
	dh $@

# Binary is pre-built by CI — just package it.
override_dh_auto_clean:

override_dh_auto_build:

override_dh_auto_install:
	install -D -m 0755 pedalboard-bridge \
		debian/pedalboard-bridge/usr/bin/pedalboard-bridge
